Slim determinate progress track.

```jsx
<ProgressBar value={68} tone="cyan" showLabel />
<ProgressBar value={40} tone="gradient" height={10} />
```

`value`/`max` define progress. `tone`: cyan/navy/gradient. `showLabel` prints the percentage. `height` in px.
