Circular avatar showing a user image or derived initials.

```jsx
<Avatar name="Ana Lima" tone="cyan" />
<Avatar name="TOTVS" src="/uploads/photo.jpg" size={56} ring />
```

`size` in px (default 40). `tone`: cyan/purple/navy/lime for the initials background. `ring` adds a cyan ring. Initials use the display font.
