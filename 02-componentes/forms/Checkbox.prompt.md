Square checkbox with a cyan fill and navy checkmark when selected.

```jsx
<Checkbox checked={ok} onChange={setOk} label="Li e aceito os termos." />
```

Controlled via `checked` + `onChange(next)`. Optional `label`, `disabled`.
