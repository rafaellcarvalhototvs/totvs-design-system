Binary on/off toggle. Cyan track + navy knob when on.

```jsx
<Switch checked={on} onChange={setOn} label="Receber novidades da TOTVS" />
```

Controlled: pass `checked` and `onChange(next: boolean)`. Optional `label`. 44px wide track.
